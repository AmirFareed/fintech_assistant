server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    # config.js holds the backend URL; never cache it so it can be edited in place.
    location = /config.js {
        add_header Cache-Control "no-store";
    }
}
