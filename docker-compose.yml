services:
  fintech-assistant:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fintech-assistant
    env_file:
      - .env
    ports:
      - "5000:5000"
    volumes:
      - ./uploads:/app/uploads
    restart: unless-stopped
